# Arrays are used to store multiple values in one single variables.
'''
NOTE:
Without the array module or NumPy, Python does not have a built-in, low-level array type. Instead, we use the built-in list type to achieve array-like functionality.
'''
lang = ["Python", "Java", "JavaScript"]
print(lang)
print(type(lang))